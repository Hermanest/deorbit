[package]
name = "deorbit"
version = "0.1.0"
edition = "2024"

[dependencies]
deorbit-core = { path = "../deorbit-core" }
deorbit-macro = { path = "../deorbit-macro", optional = true }

[features]
default = ["macros"]
macros = ["dep:deorbit-macro"]
